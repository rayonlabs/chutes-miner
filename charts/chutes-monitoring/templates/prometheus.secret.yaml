{{ if .Values.prometheus.createSecret }}
apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: prometheus
  namespace: "{{ .Release.Namespace }}"
  annotations:
    kubernetes.io/service-account.name: "prometheus-server"
{{- end }}